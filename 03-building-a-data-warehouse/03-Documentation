ออกแบบขึ้นเพื่อโหลดข้อมูลจากไฟล์ JSON ที่อยู่ในไดเรกทอรีที่กำหนดเข้าสู่ BigQuery โดยผ่านไฟล์ CSV 
ซึ่งเป็นรูปแบบที่ BigQuery สามารถอ่านได้ง่าย
## ส่วนสำคัญของโปรแกรม:
>> get_files(filepath: str) -> List[str]: ฟังก์ชันนี้ใช้สำหรับการนับและแสดงรายการของไฟล์ทั้งหมดที่อยู่ในไดเรกทอรีที่ระบุ โดยจะค้นหาไฟล์ JSON และส่งคืนรายการที่เก็บเส้นทางเต็มของไฟล์ทั้งหมดในไดเรกทอรีที่ระบุ.
>> main(dataset_id, table_id, file_path): ฟังก์ชันนี้จะโหลดข้อมูลจากไฟล์ CSV ลงใน BigQuery โดยกำหนดคอนฟิกต่างๆ เช่น การตัดข้อมูลที่อยู่ด้านหน้าของไฟล์ CSV และการกำหนด Schema สำหรับข้อมูลที่จะโหลด.
>> การออกแบบ Data Model: โครงสร้างข้อมูลที่โปรแกรมนี้ใช้เป็นสำหรับโหลดข้อมูลลงใน BigQuery ประกอบด้วยคอลัมน์ที่แทนด้วย "id", "type", "actor", "repo", และ "created_at" โดยทุกคอลัมน์มีชนิดของข้อมูลที่เหมาะสม.
>> การใช้งาน: การเรียกใช้โปรแกรมโดยระบุพารามิเตอร์ที่เกี่ยวข้อง เช่น dataset_id, table_id, และ file_path.

##การใช้งาน 
เพื่อใช้งานโปรแกรม ต้องเรียกใช้ฟังก์ชัน get_files() เพื่อระบุไดเรกทอรีที่มีไฟล์ JSON จากนั้นโปรแกรมจะสร้างไฟล์ CSV และโหลดข้อมูลจากไฟล์ JSON ลงในไฟล์ CSV สุดท้ายโปรแกรมจะโหลดข้อมูลจากไฟล์ CSV ลงใน BigQuery.

##ผลลัพท์
โปรแกรมจะโหลดข้อมูลจากไฟล์ JSON และบันทึกข้อมูลลงในไฟล์ CSV หลังจากนั้นจะโหลดข้อมูลจากไฟล์ CSV ลงใน BigQuery โดยกำหนด Dataset และ Table ที่ระบุ.